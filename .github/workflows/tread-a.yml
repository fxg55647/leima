name: TREAD A
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
permissions:
  contents: write
  pages: write

jobs:
  run:
    uses: ./.github/workflows/tread-run.yml
    secrets: inherit
