name: Notify Claude on failure

on:
  workflow_run:
    workflows: ["POIDE Code Review"]
    types: [completed]

jobs:
  notify:
    if: ${{ github.event.workflow_run.conclusion != 'cancelled' && github.event.workflow_run.head_branch == 'staging' }}
    runs-on: ubuntu-latest
    steps:
      - name: Send webhook to Claude
        env:
          HOOKDECK_URL: ${{ secrets.CLAUDE_WEBHOOK_URL }}
        run: |
          RUN_ID="${{ github.event.workflow_run.id }}"
          SHA="${{ github.event.workflow_run.head_sha }}"
          curl -s -X POST "$HOOKDECK_URL" \
            -H "Content-Type: application/json" \
            -d "{\"event\":\"code_review_done\",\"conclusion\":\"${{ github.event.workflow_run.conclusion }}\",\"run_id\":\"$RUN_ID\",\"sha\":\"$SHA\",\"branch\":\"staging\",\"message\":\"code_review.yml valmis (testi) — SHA $SHA\"}"
