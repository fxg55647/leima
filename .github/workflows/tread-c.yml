name: TREAD C
on:
  schedule:
    - cron: '2-59/5 * * * *'
  workflow_dispatch:
permissions:
  contents: write
  pages: write

jobs:
  run:
    uses: ./.github/workflows/tread-run.yml
    secrets: inherit
